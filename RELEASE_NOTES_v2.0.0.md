# Release Notes - v2.0.0

## 🎉 重大版本更新

这是一个重大版本更新，添加了多个用户请求的重要功能，显著提升了用户体验和安全性。

## ✨ 新增功能

### 1. 文件列表管理
- **添加单个文件**：新增"添加单个"按钮，可以逐个添加 PDF 文件到列表
- **文件去重**：自动检测并防止添加重复的文件
- **批量选择保留**：原有的"批量选择"功能继续保留，会替换整个列表

### 2. 删除文件功能
- **工时列表中的删除按钮**：每个文件后面添加红色"删除"按钮，可以单独移除不需要的文件
- **实时更新**：删除文件后自动刷新工时统计
- **智能显示**：只在成功解析工时的文件上显示删除按钮

### 3. 源文件安全删除
- **移至回收站**：新增"删除源文件（移至回收站）"复选框选项
- **安全可恢复**：签名成功的源文件会被移动到系统回收站而非永久删除
- **用户可选**：默认不勾选，用户可以在每次签名前自主决定
- **智能过滤**：只移动签名成功的文件，失败的文件保留在原位置
- **跨平台支持**：
  - Windows: 回收站（Recycle Bin）
  - macOS: 废纸篓（Trash）
  - Linux: 回收站（Trash）

## 🔧 技术改进

### 依赖更新
- 添加 `trash` crate (v5.2.6) 用于跨平台回收站功能

### 新增后端命令
- `delete_file_cmd`: 将文件移动到系统回收站的 Tauri 命令

### UI/UX 优化
- 优化文件选择区域布局
- 改进按钮样式和交互反馈
- 增强工时统计列表的可操作性

## 🎨 界面变化

- 步骤1的按钮从单个"浏览 PDF…"改为"添加单个"和"批量选择…"两个按钮
- 工时统计列表每个文件项添加红色"删除"按钮
- "开始签名"按钮后添加"删除源文件（移至回收站）"复选框

## 📦 构建信息

- 前端：TypeScript + Vite 6.4.3
- 后端：Rust + Tauri 2
- 版本号：从 v1.0.6 升级至 v2.0.0

## 🙏 致谢

感谢用户反馈和建议，这些功能的实现都是基于实际使用需求。

---

**完整变更记录**: v1.0.6...v2.0.0
